# This is the main entrypoint into the program
# It requires the other files/gems that it needs

require 'pry'
require './candidates'
require './filters'

## Your test code can go here

# @candidate = experienced?(@candidates[0])
# pp @candidate

# @FindCandidateByID = find(5)
# pp @candidates[0][:id]
# pp @FindCandidateByID

# @qualified = qualified_candidates(@candidates)
# pp @qualified
# pp @qualified.length

# @order = ordered_by_qualifications(@candidates)
# pp @order
# pp @order.length
